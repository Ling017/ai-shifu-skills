先向用户呈现本节观点：AI 让写代码和加功能变快，但一次改动跨越的业务、交互、提示词、数据和部署范围也会更大；速度越快，越需要一次只改一小部分。

创建封面幻灯片“AI 写得越快，为什么版本反而必须切得越小”。

===方向正确的大功能，是否应该一次把最终方案做完？===
?[先做完再统一验收 | 一次只验证一个想法，分步推进 | 按页面分工同时上线 | 先定完整数据结构再验证]

根据答案讲清小版本的目标：这一版只验证一件事，能单独判断结果；尽量能单独上线，并说清失败时停止什么、哪些部分可以恢复。

分步修改能减少同时变化的因素，不只是分页面或工时；一旦写数据或发消息，就必须另列已经产生的后果，不能用回退代码代表全部撤销。

创建“黄小璨 AI 一次改太多的后果”幻灯片：目标任务第一版同时改变大量文件和多个相互关联的环节；后续合并横跨多个模块和补丁；内容改造继续增加功能后出现真机重复；最终从稳定版本重起，只搬验证过的底层模块。

在图后按三个判断解释：
- 正确动机也要限制范围，多层同时变化不利于判断原因；
- Git 记录代码历史、PR 帮助大家看清这次到底改了什么、模块化减少规则复制；
- 代码审查核对失败路径和责任，已经验证稳定的版本保留可核查的代码与配置状态，数据恢复仍需单独证据。

纠正两个误区：“AI 写得快就可以扩大单次需求”和“代码重复对 AI 不重要，所以结构也不重要”。

创建“这次修改会影响哪些地方”幻灯片：业务规则、用户交互、提示词、路由或流程、数据结构、权限、部署与监控。

每次改动开始前，都标出会影响哪些部分；同时触碰层数越多，越需要拆分、增加代码审查，或说清哪些部分需要一起恢复到旧版本。

在图后讲清代码审查的业务问题：
- 失败时会留下什么半成品；
- 同一请求重复执行会不会重复创建；
- 数据属于谁；
- 更新条件会不会误伤其他用户；
- 发生错误后是否会报出来；
- 新功能是否破坏原能力。

说明AI 辅助的代码审查可以发现一些常见问题，但高风险决策仍要由具备责任的人确认。

创建“工程术语的业务翻译”幻灯片：
- Git 记录并帮助恢复代码版本，不自动恢复业务数据；
- PR 让人审阅改了什么和为什么；
- 自动测试检查它覆盖到的规则是否被改坏；
- CI/CD 自动执行检查与交付步骤，只有配置并执行了阻断规则才能拦住失败；
- 代码审查尝试提前发现错误假设，不能保证发现所有问题。

创建对照幻灯片：一次上线完整客户经营平台，与三步验证“先生成建议—再保存待确认草稿—最后小范围触达”。

比较查找故障原因的难度、反馈速度、回退范围和数据风险。

贯穿一个简化故障：把草稿保存给了错误客户。

关闭保存按钮只防止继续保存，回退代码只改变后续程序行为，原有错误记录仍需单独核对处理；已发送的消息还需要额外补救。

第 10 节继续用此例区分数据恢复。

创建“怎样把一个大愿望说成一次可检查的改动”幻灯片，比较两种需求表达：
- 含糊需求：把客户跟进自动化做完整。
- 有边界的需求：本版只验证根据客户记录生成待确认草稿。先说明会改哪些地方、是否写入客户数据、怎样判断成功、失败后怎样处理；自动发送或权益修改需要另行确认。
图后解释业务人提出改动时，要说清本次目标、影响范围、验收依据和需要另行确认的动作。先出影响说明和方案，再按已确认范围实施；Git、PR、测试用于留下和核对证据，写进需求不代表保障已经生效。用这个标准帮助学员判断已有版本拆解，不要求操作 Git、编写代码或提交真实开发需求。

===请把当前小范围业务验证拆成三个连续的小验证。===

?[%{{version_slice}} ...每一步写清只验证什么、观察什么、失败撤回什么]

根据 {{version_slice}} 检查是否仍按页面数量或功能列表拆分；帮助改为按业务假设和风险拆分，并指出哪一步开始写数据或影响用户。
从学员的第一步中提炼一段可与 AI 或技术团队讨论的改动描述，检查本次目标、影响范围、验收依据和需另行确认的动作。保留合理方案，只对最关键的缺口给一个补充提示；新增内容标为课程建议，未知的实现范围、授权和恢复能力保留待确认，不再布置一轮改写作业。

===哪种情况说明不应继续零散修补，而要回到已经验证稳定的版本重新判断？===

?[单一页面文字错误 | 多个规则和模块互相冲突且无法解释真实行为 | 新功能还没设计图 | 开发速度暂时变慢]

根据答案说明多个部分互相影响的问题，要弄清所有受影响的地方再处理，条件不足时先暂停修改；从已经验证稳定的版本重起不是推倒重来，而是先停下零散修补，基于可靠版本重新安排改动。

输出简短《分步验证记录》，项目只取学员描述 {{project_context}}，不使用黄小璨 AI 作为其项目名。

引用 {{version_slice}}，标出每步判断依据与开始写数据或影响客户的位置，把停止功能、回退代码、处理已有数据或外部动作分开；未提供的回退能力和负责人待确认。

不生成数据库结构、接口实施或开发排期。
